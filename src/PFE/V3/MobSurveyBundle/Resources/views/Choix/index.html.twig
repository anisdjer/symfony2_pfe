{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Choix list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Valeur</th>
                <th>Chtype</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('choix_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.valeur }}</td>
                <td>{{ entity.chType }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('choix_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('choix_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('choix_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
