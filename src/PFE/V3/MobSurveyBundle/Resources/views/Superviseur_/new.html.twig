{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Superviseur creation</h1>

    <form action="{{ path('users_superviseur_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('users_superviseur') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
